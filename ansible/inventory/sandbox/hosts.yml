---
# Sandbox inventory
# Populate VM IPs from Terraform outputs after applying the sandbox environment.
# Example: cd terraform && terraform output -json

all:
  children:
    sandbox:
      hosts:
        # Add sandbox VMs here after deploying with Terraform
        # sandbox-test-01:
        #   ansible_host: 192.168.20.100
        #   ansible_user: ubuntu
