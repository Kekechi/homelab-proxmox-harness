---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart step-ca
  ansible.builtin.systemd:
    name: step-ca
    state: restarted

- name: Restart nginx
  ansible.builtin.systemd:
    name: nginx
    state: restarted
